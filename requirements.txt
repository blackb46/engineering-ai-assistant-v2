# ============================================================
# requirements.txt
# City of Brentwood Engineering AI Assistant - V2
# ============================================================
# Streamlit Cloud installs these automatically on deployment.
# Pin versions to prevent surprise breakage from upstream updates.
# Last verified working: March 2026
# ============================================================

# ── AI and Vector Database ───────────────────────────────────
anthropic==0.49.0
chromadb==0.6.3
sentence-transformers==3.4.1

# ── Web Framework ────────────────────────────────────────────
streamlit==1.43.2

# ── Document Processing (corpus build only) ──────────────────
python-docx==1.1.2

# ── Data Handling ────────────────────────────────────────────
pandas==2.2.3

# ── Google Integration ───────────────────────────────────────
# gspread + google-auth — Google Sheets (flagged response logging)
gspread==6.1.4
google-auth==2.38.0

# google-api-python-client — Google Drive (database download)
# NEW in V2: required by drive_loader.py
google-api-python-client==2.161.0
