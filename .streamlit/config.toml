[theme]
primaryColor = "#1E88E5"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F5F5F5"
textColor = "#212121"
font = "sans serif"

[server]
headless = true
enableCORS = false
enableXsrfProtection = true
```

6. Click **"Commit changes"**

---

After this, your structure should be:
```
.streamlit/
    config.toml    ← Just one level, not nested
